# ZCHAT Regression Suite

**285 fixed bugs** distilled from the full project history — every one must stay fixed.
Tier 1 = **154** (money / crypto / crash / messaging-integrity / security — a regression here loses funds, leaks plaintext, drops messages, or bricks launch).
Tier 2 = **131** (features / UX / nav / theme).
💵 = touches money or crypto.

## How to use
- **Every build:** run the JVM unit suite (`./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest`, currently 495 tests) + all **Tier 1** device/endpoint checks below.
- **Every release:** Tier 1 **and** Tier 2.
- Check a box only after you've re-observed the fix (device action, unit test, or endpoint), not from memory.
- New bug fixed → **add a row here** in the same session, so it's never re-fixed twice.
- Source tag points at the memory file with the full root-cause writeup.

_Generated 2026-07-12 from 35 memory-history files via Fable extraction. Keep it current._

---

## TIER 1 — Critical (run every build)

### 💰 Money / on-chain send  ·  22 case(s)

- [ ] **Replies+reactions forced ON-CHAIN (charged) in TUNNEL/OPEN (C1/B4b)**  💵 — TUNNEL conversation: note wallet balance, send a reply and a reaction → both deliver instantly over NOSTR, NO new on-chain transaction and balance unchanged.  _(bug_campaign `v2.11.0`)_
- [ ] **ZEC DRAIN: TUNNEL messages silently charged on-chain (F1)**  💵 — Fresh TUNNEL conversation BEFORE peer npub is known: note balance, send a message → 'Waiting for secure connection…' bubble, NO on-chain tx, balance unchanged; after the peer's bootstrap arrives the queued message flushes over NOSTR. Code-…  _(bug_campaign `v2.11.0`)_
- [ ] **Read-receipts/status/payment-requests charged on-chain in TUNNEL/OPEN (F2)**  💵 — TUNNEL conversation: read incoming messages and create a payment request → no new on-chain transactions appear and balance is unchanged; in VAULT they still go on-chain.  _(bug_campaign `v2.11.0`)_
- [ ] **ZIP-321 multi-chunk memo payment overpay (~N x amount)**  💵 — Run the overpay regression test added in the ui-lib JVM suite (./gradlew :ui-lib:test...): buildZip321Uri with a memo spanning 3+ chunks must place the full amount on output 1 only and DUST on continuation outputs, with totalCost = amount …  _(deep_audit_jul9 `39264ff28`)_
- [ ] **BUG#5: inbound payment request over NOSTR rendered as raw ZREQ text**  💵 — Device: from Seeker in Tunnel mode send a Payment Request for 0.0003 ZEC to Honor; Honor must render 'Payment Requested — 0.0003 ZEC — Pay' (not raw ZREQ/... text) and tapping Pay must start fulfilment. Also spot-check the drift class: sen…  _(deeptest_bugs4_5 `caa774634`)_
- [ ] **Advanced money sends missing TOFU key-change gate**  💵 — Unit preferred: simulate a stored-key mismatch (blockedByKeyChange=true) for a peer, then invoke each of sendScheduled/sendBlockLocked/sendConditional/unlockPayment/unlockConditional/sendPaymentRequest -> ALL must refuse with the key-chang…  _(first_contact_kex_fix)_
- [ ] **OPEN send fell through to CHARGED on-chain memo when NOSTR publisher not ready (CRITICAL)**  💵 — Device: in an established OPEN chat, make relays unreachable (airplane mode with wallet already synced, or block wss://relay.zsend.xyz), send a text message -> the send must FAIL with a visible error and NO spend proposal / no new tx in wa…  _(free_open_msg1 `#224`)_
- [ ] **Reaction in OPEN mode charged an on-chain tx (MEDIUM)**  💵 — Device: in an established OPEN chat, add a reaction -> confirm NO spend proposal / no new tx in wallet history, and the reaction arrives on the peer over NOSTR (relay publish logged, wallet balance unchanged).  _(free_open_msg1 `#224`)_
- [ ] **Payment phantom success on biometric cancel**  💵 — On device: start a payment from chat, when the biometric/PIN prompt appears press back/cancel -> UI must show cancelled/error, NOT success; wallet balance unchanged and no tx broadcast. Code-level: verify the cancel path throws SpendAuthCa…  _(messaging_recovery_batch `797fe396b`)_
- [ ] **Payment wedged on 'Sending' forever (biometric replay-0 race)**  💵 — On device: perform 3-5 consecutive payments-from-chat with biometric/PIN auth in quick succession -> every one completes to Success or Error, none remains on 'Sending'. Code-level: assert BiometricRepository shared flow uses replay=1 and c…  _(messaging_recovery_batch `797fe396b`)_
- [ ] **F1: Basic send path charged on-chain in non-VAULT modes (ZEC drain layer 1)**  💵 — On Honor+Seeker pair, set a chat to TUNNEL (and separately OPEN), note wallet balance, send a normal text message. Expect logcat 'ZCHAT_NOSTR: published to N relay(s)', NO cost dialog, NO tx proposal, balance unchanged. Then switch same ch…  _(money_drain_and_rotation `F1`)_
- [ ] **Six advanced send functions bypassed the NOSTR/mode gate and always went on-chain (ZEC dr…**  💵 — In a non-VAULT (TUNNEL or OPEN) chat on Honor, attempt each of the 6 advanced sends: scheduled message, block-locked, payment-locked, conditional, unlock-payment, unlock-conditional. Each must show the 'works only in Vault chats' message, …  _(money_drain_and_rotation)_
- [ ] **KEXACK paid on EVERY received KEX with no dedup (ZEC drain layer 3)**  💵 — Two-device: force duplicate KEX delivery (background/foreground the peer app so NOSTR re-delivers, or trigger peer re-KEX with the SAME key). Grep Honor logcat around sendKEXAckMessage: exactly ONE paid KEXACK for the (peer, pubkey) pair, …  _(money_drain_and_rotation)_
- [ ] **#221: 'Free'-labeled AI model still charged the user (backend billing, fix UNCOMMITTED)**  💵 — In /home/yourt/zchat: FIRST verify the fix is still present in server.ts (it was never committed — check git status/diff for the isFreeTier gating). Then run 'pnpm test' — the #221 regression test in server.test.ts must pass: free-tier cha…  _(open_mode_and_group_epoch)_
- [ ] **wallet-core CLI hardcoded pre-NU6.2 consensus branch id (BranchId::Nu6)**  💵 — grep -n 'BranchId::' /home/yourt/zchat/packages/wallet-core/src/lib.rs — must show Nu6_2, never plain Nu6; then ssh_moltbot and check ai-topup-watcher / zchat-backend logs for absence of branch-id (0x5437f330) warnings; optionally run the …  _(orchard_vuln_audit)_
- [ ] **Orphaned pending-send: VAULT message stuck 'Sending after confirmation' forever after res…**  💵 — 2-device (Honor<->Seeker, debug builds): send VAULT message A, immediately send VAULT message B so it QUEUES behind A (single-note constraint); force-stop the app (adb shell am force-stop xyz.zsend.zchat.foss.debug) before B broadcasts; re…  _(orphaned_pending_send)_
- [ ] **Mint-vs-restore seed-overwrite race (fund loss)**  💵 — Code/unit: assert restoreWallet() acquires onboardingMintMutex before touching the persisted wallet (grep the acquisition or a unit test forcing mint+restore concurrency). Device sanity: start new-wallet creation then immediately perform a…  _(security_audit_2week `9da495c04`)_
- [ ] **#235 KEXACK re-spend drain — ~1000 zatoshi charged on every cold start for VAULT/legacy r…**  💵 — Device: on a VAULT/legacy contact whose KEXACK already succeeded, force-stop and relaunch ZCHAT, open the chat; confirm NO new on-chain send occurs (no new ~1000-zatoshi tx in wallet history, logcat shows the sentkexack guard skip). Repeat…  _(tunnel_cold_first_contact_deadlock `88415831c`)_
- [ ] **#234 False 'Free — sent over NOSTR' label while send actually spends ZEC (cold-launch win…**  💵 — Device: force-stop ZCHAT, cold-launch, and immediately open an established Tunnel chat before the NOSTR publisher registers: the bar must show the on-chain cost (NOT 'Free') and only flip to 'Free — sent over NOSTR' once outbound is ready;…  _(tunnel_cold_first_contact_deadlock `88415831c`)_
- [ ] **#201 anti-flap: historical KEX txs re-handled every rescan (false key-change + drain)**  💵 — Code: zchat_kex_seen prefs + hasProcessedKexTx/markKexTxProcessed (LRU 500, cleared in clearAll) exist, and handleKEXMessage does the ATOMIC check-then-mark UP FRONT before the when-branches. Device: force-stop and relaunch a wallet with h…  _(tunnel_kex_bootstrap_deadlock `#201`)_
- [ ] **#221 AI free-tier models charged non-zero credits**  💵 — Backend (moltbot zchat-backend + zchat-postgres): create/use a test account, make an AI call against a model whose AiModelPricing marks it free-tier; query the ledger and assert the AiUsageEvent charge is 0 and the AiAccount balance is unc…  _(v2_11_ship_and_criticize `backend f5cf954`)_
- [ ] **Double-based ZEC→zatoshi conversion loses precision at send sites**  💵 — Unit: assert "0.00000003".toBigDecimal().convertZecToZatoshi() yields exactly 3 zatoshi (and a sweep of sub-1e-6 ZEC edge values round-trips exactly). Static regression: grep zchat-android app sources for `* 100_000_000` / `100000000).toLo…  _(zatoshi_precision)_

### 🔐 Crypto / E2E / ratchet  ·  35 case(s)

- [ ] **Destroy-all crashed AND did not wipe crypto/contacts (Bug 5)**  💵 — On a throwaway wallet: chat-list red Destroy → PIN → app wipes and restarts with NO crash; after relaunch verify no conversations, no contacts, and ratchet/NOSTR prefs empty (fresh identity). Unit: DestroyManagerTest if the Robolectric har…  _(bug_campaign `v2.11.0`)_
- [ ] **clearAll() skipped modePrefs — NOSTR identity survived destroy**  💵 — Unit: call ZchatPreferences.clearAll() then read mode:<peer> and NOSTR identity entries → all empty. Device: destroy-all, re-onboard → npub differs from the pre-wipe npub.  _(bug_campaign `v2.11.0`)_
- [ ] **clearAll() skipped ratchetPrefs + viewOncePrefs**  💵 — Unit: seed ratchet state + a viewOnce mark, call clearAll() → both stores empty. Re-run the JVM prefs/ratchet-store suites.  _(bug_campaign `v2.11.0`)_
- [ ] **Replies sent PLAINTEXT in E2E conversations**  💵 — In an E2E (Vault) conversation send a reply, then inspect the raw memo/debug log for that tx → ciphertext envelope (E2E1:), never the plaintext body.  _(bug_campaign `v2.11.0`)_
- [ ] **Key-change (MITM signal) did not block sending (B8)**  💵 — Simulate a peer key-change (banner shown): sendMessage/reply/reaction/payment are all blocked until the banner is dismissed; verify a ZBOOT handshake still goes through while blocked.  _(bug_campaign `v2.11.0`)_
- [ ] **KEX/KEXACK self-signed key-substitution overwrites established peer key**  💵 — Unit: deliver a validly self-signed KEX carrying a NEW pubkey for an already-established peer; assert the stored E2E key is unchanged, the key-change flag is raised, and no convId remap occurs. Also assert first-contact (no stored key) and…  _(deep_audit_jul9 `41426c6b9`)_
- [ ] **Decrypted message text stored in plaintext SharedPreferences**  💵 — Device: after sending/receiving group and pending messages, run `adb shell run-as xyz.zsend.zchat.foss.debug sh -c 'cat shared_prefs/*.xml'` and grep for any message plaintext — text must appear only in the distinct `_encrypted` pref files…  _(deep_audit_jul9 `41426c6b9`)_
- [ ] **Memo/ciphertext content and convId leaked to logcat** — Device: capture `adb logcat` while sending and receiving an E2E message on the Honor<->Seeker pair, then grep the capture for the exact message text, the memo body, and the convId — none may appear. Source check: the two previously-offendi…  _(deep_audit_jul9 `41426c6b9`)_
- [ ] **MED-A: unauthenticated KEXACK convId remap**  💵 — Unit: (a) KEXACK where heldKey != parsedAck.publicKey -> assert setConversationId is NOT called; (b) heldKey == null (first contact) and heldKey == parsedAck.publicKey (legit #205 UA-drift re-link) -> assert remap DOES proceed. Both branch…  _(deep_audit_jul9 `159b0fc40`)_
- [ ] **BUG#1: plaintext leak on shielded chat**  💵 — Device: send a message in a shielded (VAULT) chat Honor->Seeker; inspect the outbound on-chain memo (logcat send-path dump or block explorer for the tx) and verify the memo body is E2E ciphertext (E2E1: prefix), with the raw text appearing…  _(deeptest_bugs4_5 `b11389fd4`)_
- [ ] **Message-request accept could bind a NOSTR pubkey already bound to another peer (MEDIUM)**  💵 — Unit: seed prefs with pubkey:<peerA>=K, then acceptMessageRequest for a request from pubkey K claiming a different address -> must be rejected with the pubkey-reuse result and peerA's binding unchanged.  _(free_open_msg1 `#224`)_
- [ ] **Contact-QR scan silently overwrote a stored peer NOSTR key (MEDIUM)**  💵 — Device or unit: with pubkey:<peer>=K1 stored, scan/paste a zchat:c1?z=<same addr>&n=<different 64-hex K2> code -> must raise the key-change warning and NOT silently overwrite K1; same-key rescan remains a no-op.  _(free_open_msg1 `#224`)_
- [ ] **KEX/KEXACK signature verification bound to mapped-sender address string (drift class bugs…**  💵 — Unit: run the KEX/KEXACK signature-verification JVM tests confirming verification uses the payload-embedded senderAddress (E2EEncryption ~L629 signing over senderAddress+publicKey) and still rejects signatures from the wrong key. Device: f…  _(group_invite_compact)_
- [ ] **Messaging dead both ways after wipe/restore (ratchet desync) - no recovery path**  💵 — 2-device (Honor+Seeker): induce desync (wipe+seed-restore one device), confirm `W/ZCHAT_E2E: Ratchet decrypt failed ... AEADBadTagException` in logcat; run overflow-menu Reset Encryption on BOTH sides; wait for 'E2E key exchange COMPLETE';…  _(messaging_recovery_batch `797fe396b`)_
- [ ] **Plaintext-send window during Reset Encryption (send-path fails open)**  💵 — 2-device: run Reset Encryption on one side only, then immediately try to send a normal message AND a reply before re-KEX completes -> both must abort with an error; capture the peer chain/NOSTR traffic and assert no plaintext ZMSG memo lea…  _(messaging_recovery_batch `0e0fa40eb`)_
- [ ] **KEX initiator left with e2eEnabled=false (header 'Off' + plaintext risk)**  💵 — 2 fresh-paired devices: have device A initiate the KEX; after the KEXACK arrives on A, verify A's chat header shows E2E ON and A's next message goes out E2E1-encrypted (logcat/on-chain memo check), not plaintext.  _(messaging_recovery_batch `0e0fa40eb`)_
- [ ] **CRITICAL: VAULT message sent as raw on-chain plaintext (e2eEnabled stuck false)**  💵 — On a chat with an established shared key, force prefs e2eEnabled=false (reinstall artifact or direct prefs edit on debug), then send a VAULT text -> logcat must show the 'Self-healed ... must encrypt, not plaintext' line and the outgoing m…  _(messaging_recovery_batch `b11389fd4`)_
- [ ] **OPEN to VAULT mid-conversation switch breaks decryption**  💵 — 2-device: establish an OPEN conversation, then switch the sender to VAULT and send -> receiver must decrypt the VAULT message (no 'decryption key isn't on this device'), sender must remain able to send, and no revert-to-OPEN needed. Repeat…  _(mode_flow_bug_batch `e3f111ab4 + 37a2e5cb3 (B1/B2)`)_
- [ ] **Reset Encryption regression (silent, sender wedged, no recovery)**  💵 — 2-device (Honor+Seeker), the exact validated scenario: run Reset Encryption -> exactly ONE KEX goes out, sender is NOT wedged (can send once handshake completes), and the peer decrypts a fresh post-re-KEX message (convergent-root heal). Th…  _(mode_flow_bug_batch `c36c7f6b1 (B6)`)_
- [ ] **QS-BRIDGE: QuantumShieldScanBridge stayed armed after backing out of scanner**  💵 — On Honor: open peer A's Quantum Shield → 'Scan Peer's QR' → back out of the scanner WITHOUT scanning; then open peer B's chat and run any scan flow. Logcat must show the bridge was cleared on back-out and no QS completion is attributed to …  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **#222: Seed words readable in accessibility tree while visually blurred (pre-biometric)**  💵 — On Honor: open More → Backup/Secret Recovery Phrase, do NOT pass biometric reveal, run 'adb shell uiautomator dump' and pull the XML — it must show 24 numbered slots but ZERO seed words. Then reveal via biometric and confirm the words rend…  _(open_mode_and_group_epoch `3ff57de5f`)_
- [ ] **Vulnerable halo2/orchard crates in wallet-core (latent Orchard soundness bug)**  💵 — cd /home/yourt/zchat && grep -A2 '^name = "halo2_gadgets"' Cargo.lock (expect version >=0.5.0) && grep -A2 '^name = "orchard"' Cargo.lock (expect >=0.14.0); run cargo audit — the Orchard advisories GHSA-ghc3-g8w4-whf9/GHSA-jfw5-j458-pfv6 m…  _(orchard_vuln_audit)_
- [ ] **Ratchet DoS via forged own-direction memo with huge counter**  💵 — cd /home/yourt/zchat-android && ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest --tests '*forged_own_direction_huge_counter_rejected_fast*' — the regression test must pass (forged own-direction memo with a huge counter is rejected fast…  _(orchard_vuln_audit)_
- [ ] **#229 ZBOOT replay-downgrade — stale on-chain ZBOOT can revert live pubkey**  💵 — Unit: run ZBootMessageTest (v3 serialize, epoch-bound signedData, non-zero-epoch round-trip, legacy-v2 epoch-0, negative/non-numeric epoch rejected) and MessageRouterTest v3 expectation. Device: rotate on Honor, confirm Seeker's bootepoch:…  _(rotation_unread_fixes `f5afbd1b9`)_
- [ ] **#190 Contact book stored in plaintext SharedPreferences**  💵 — Device (Honor): run-as the app package — shared_prefs/zchat_contact_book_enc.xml must exist with opaque ciphertext keys/values (no plaintext u1... address or nickname visible); legacy zchat_contact_book.xml must have its contacts key scrub…  _(security_hardening_batch)_
- [ ] **#205 KEXACK signature rejected on self-address representation drift (payload-address fall…**  💵 — Log-based device check during any fresh 2-device handshake: logcat must show the fallback path 'KEXACK signed with a different self-address representation than stored ... accepted via payload-address verify' followed by 'E2E key exchange C…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_
- [ ] **KEXACK signature verified against wrong (stale) address representation**  💵 — Code: both KEX and KEXACK receive branches verify against the convId-mapped address FIRST then FALL BACK to the self-signed payload address via parseKEX*PayloadFull(payload, null). Device (regression signature): 2-device handshake watcher …  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **Messages sent PLAINTEXT in Zcash memos (E2E encryption never invoked)**  💵 — Unit: run E2EMessageProcessorTest (4) + CiphertextWireFormatTest (6) via ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest. Code: doSendMessage must call the message processor's encryptOutgoing BEFORE createChunkedMessageProposal and hav…  _(v2_11_0_session `v2.11.0`)_
- [ ] **GCM nonce reuse risk from async ratchet-state persistence**  💵 — Code: EncryptedPrefsRatchetStateStore must use .commit(), not .apply(). Unit: run E2ERatchetTest (17 tests, includes restore-sim and counter advancement) via ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest --tests "*E2ERatchetTest*".  _(v2_11_0_session `v2.11.0`)_
- [ ] **Own outgoing messages triggered replay detection on blockchain re-scan**  💵 — Unit: E2ERatchetTest 'own-outgoing-no-replay' case must pass: ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest --tests "*E2ERatchetTest*". Device: force a rescan on a device with sent-message history and confirm own messages still rende…  _(v2_11_0_session `v2.11.0`)_
- [ ] **messageProcessors HashMap not thread-safe**  💵 — Code: getOrCreateMessageProcessor in ChatViewModel still wraps lookup+create in synchronized(messageProcessors). Unit: E2ERatchetTest 'concurrent-encrypts-monotonic' passes.  _(v2_11_0_session `v2.11.0`)_
- [ ] **Safety number computed over Base64 strings instead of raw key bytes**  💵 — Unit/code: safety-number computation decodes to raw bytes and hashes min//max ordering. Device (2-device): open the Safety Number dialog on both Honor and Seeker for the same pair — both must display the identical 32-hex fingerprint.  _(v2_11_0_session `v2.11.0`)_
- [ ] **TOCTOU race in message-processor cache creation**  💵 — Code: the entire null-check + create + put in getOrCreateMessageProcessor is inside one synchronized(messageProcessors) block. Unit: E2ERatchetTest 'concurrent-encrypts-monotonic' passes.  _(v2_11_0_session `v2.11.0`)_
- [ ] **Encrypt failure silently fell back to sending plaintext**  💵 — Code: audit doSendMessage — on encryptOutgoing throw the send must abort (no branch that passes the raw text to createChunkedMessageProposal). Unit: E2EMessageProcessorTest covers the failure path. Device: force an encrypt failure (e.g. mi…  _(v2_11_0_session `v2.11.0`)_
- [ ] **Pruned seen-set allowed replay of old counters**  💵 — Unit: E2ERatchetTest replay + out-of-order + skip-backfill + max-skip-rejected cases all pass: ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest --tests "*E2ERatchetTest*".  _(v2_11_0_session `v2.11.0`)_

### 💥 Crash / stability  ·  10 case(s)

- [ ] **Dark-screen crash on Close after ZEC transfer (Bug 1)** — On device with funds: complete a ZEC send → transaction detail → tap Close/back → must land on home screen, never a dark screen; repeat with hardware back.  _(bug_campaign `v2.11.0`)_
- [ ] **BUG#4b: hardcoded FileProvider authority crashes Share/Forward on flavor builds** — Device on the foss debug flavor: long-press a received file -> Share (and Forward) -> the share sheet must open with no 'Couldn't find meta-data for provider' crash. Source check: `grep -rn 'xyz.zsend.zchat.provider'` returns no hardcoded …  _(deeptest_bugs4_5 `69e7ff8cd`)_
- [ ] **CallLogType.valueOf crash on malformed persisted call-log data** — Unit: feed an invalid/unknown type string into call-log deserialization -> no exception (entry skipped or defaulted). Device alternative: write a corrupt call-log entry into the app's persisted store via run-as, open call history -> no cra…  _(first_contact_kex_fix)_
- [ ] **Cold-launch NPE crash from avatarBroadcastInFlight init-order** — On a RELEASE build: set a self-avatar in Settings, relaunch once so it persists (survives adb install -r). Then loop 10+ times: adb shell am force-stop <zchat pkg> ; am start ; sleep 5 ; verify pidof <pkg> is alive AND `adb logcat -b crash…  _(init_order_crash `v2.12.4, commit 28713c7b0 (versionCode …`)_
- [ ] **Deterministic crash entering Restore Wallet after any seed-QR scan** — On device: scan a valid seed-backup QR using the address scanner (ScanZashiAddressVM routes it to onRestoreSeedScanned), then open Restore Wallet screen -> must render without crash; `adb logcat -b crash -d` clean. Static check: seedWords …  _(init_order_crash `v2.12.4, commit 28713c7b0`)_
- [ ] **Latent init-order fields in ChatViewModel incl. KEXACK dedup maps**  💵 — Static regression check: grep ChatViewModel.kt and assert replyRefSentinel, replyRefPrefix, replyPreviewSep, tunnelPendingPayloads, kexAckedKeys, kexAckInFlight are all declared before the init{} block; broader sweep: no eagerly-initialize…  _(init_order_crash `v2.12.4, commit 28713c7b0`)_
- [ ] **Signed-release crash: secp256k1 JNI library fails to load (onboarding-fatal)** — Build ./gradlew :app:assembleZcashmainnetFossRelease, verify the built APK manifest has android:extractNativeLibs=true (aapt dump / apkanalyzer) and app/build.gradle.kts still has useLegacyPackaging = true; install the signed release (xyz.…  _(release_build_requirements)_
- [ ] **Signed-release crash at launch: Twig.assertLoggingStripped canary fires when minify is OFF** — Confirm gradle.properties IS_MINIFY_ENABLED=true and the release build command is plain ./gradlew :app:assembleZcashmainnetFossRelease (no minify-off override); install the release on-device and confirm it launches past Application.onCreat…  _(release_build_requirements)_
- [ ] **Minified release breaks JNI-bound classes without keep-rules (secp256k1/WebRTC/zxingcpp)** — grep -E 'fr\.acinq\.secp256k1/org\.webrtc/zxingcpp' /home/yourt/zchat-android/app/proguard-project.txt — all three keeps present; then on a freshly built minify-ON release installed on-device: complete CREATE (exercises secp256k1) and plac…  _(release_build_requirements)_
- [ ] **READY-with-no-wallet crash-brick**  💵 — Unit: completeOnboarding suspends until the persisted wallet is non-null before writing READY. Device: kill the app immediately after tapping the final onboarding confirm (adb force-stop within ~1s); relaunch must resume onboarding or land…  _(security_audit_2week `9da495c04`)_

### ✉️ Messaging delivery  ·  39 case(s)

- [ ] **Post-restart ratchet conversation wedge (sessionSeen lost on restart)** — Unit: run './gradlew :ui-lib:testZcashmainnetFossDebugUnitTest' and confirm the E2ERatchet high-water-mark tests are green (full suite was 458+3 new). Device: on an established Honor↔Seeker E2E chat, exchange several messages both ways, fo…  _(audit_fixes_jul9_fable `948859be7 + 0d8f90966`)_
- [ ] **O(counter) HMAC chain-walk DoS via forged first-of-session message** — Unit: run the existing JVM test counter_above_max_skip_rejected_as_dos_protection in :ui-lib:testZcashmainnetFossDebugUnitTest — it must pass UNMODIFIED (it caught the regression originally and passes unmodified against the high-water fix)…  _(audit_fixes_jul9_fable `0d8f90966`)_
- [ ] **convID-collision message misroute** — 2-device: run two active conversations concurrently and exchange messages in both — every message lands in its own thread; grep logcat ZCHAT_CONVID for misroute/inconsistent-mapping warnings (expect none for fresh convs).  _(audit_pending_items)_
- [ ] **View-once media not consumed on reveal** — On device: receive a view-once image, open it, close it, tap again → content no longer viewable (consumed state persists across app restart).  _(audit_pending_items)_
- [ ] **Genuine ZCHAT message flagged 'Unknown sender' (Bug 7)** — Unit: the 9 androidTest recognition tests including the 3 'still-flags-spoofed' security tests. Device: restart app, receive a message from a known peer → no Unknown-sender banner; a plain (non-ZMSG) memo still flags.  _(bug_campaign `v2.11.0`)_
- [ ] **Emoji/non-BMP characters corrupted in chunked messages** — JVM test: substringByBytes/packChunks with multi-emoji input → no split surrogates, every chunk <=512 bytes, round-trip equals input. Device: send an emoji-heavy multi-chunk message on-chain → renders intact on the receiver.  _(bug_campaign `v2.11.0`)_
- [ ] **ZCALL prefix injection swallowed real chat messages** — Send a chat message literally starting with 'ZCALL/v1/garbage' over NOSTR → it renders as normal text on the receiver, no call UI triggered.  _(bug_campaign `v2.11.0`)_
- [ ] **Chunk-count desync silently truncated emoji messages (regression of emoji fix)** — JVM test: for emoji-heavy inputs assert calculate*ChunkCount == packChunks(...).size and full reassembly equals input; send a long emoji message on-chain and confirm no tail loss.  _(bug_campaign `v2.11.0`)_
- [ ] **Unbounded file download — attacker-controlled OOM** — Unit/manual: a ZFILE declaring >25MB is rejected before download; a body exceeding the declared size is cut at the cap (no OOM, WARN logged).  _(bug_campaign `v2.11.0`)_
- [ ] **Composed message LOST when send rejected (B1)** — Trigger a rejected send (e.g. NeedsOrchardShielding or active key-change banner) → the typed text REMAINS in the input field.  _(bug_campaign `v2.11.0`)_
- [ ] **NOSTR dedup collapsed distinct same-text messages (B7)** — 2-device TUNNEL: send the exact same text twice within one second → BOTH bubbles appear on the receiver; the same event via two relays still shows once.  _(bug_campaign `v2.11.0`)_
- [ ] **ZBOOT prefix-spoof rendered as secure-connection request (B11)** — Send/receive a plain text starting with 'ZBOOT/notreal' → renders as raw text, NOT the 🔐 secure-connection label; a real ZBOOT still shows the label.  _(bug_campaign `v2.11.0`)_
- [ ] **Reply-quote preview leaked locked-message plaintext (C3)** — Reply to a payment-locked (unpaid) message → the quote preview shows '🔒 Locked message', never the underlying text; check bubble, compose preview, and fullscreen sites.  _(bug_campaign `v2.11.0`)_
- [ ] **Notification leaked locked-message plaintext** — Background the app, receive a payment-locked message → the notification shows a generic/locked label, not the hidden content.  _(bug_campaign `v2.11.0`)_
- [ ] **NOSTR messages not persisted — disappeared on reload (F4)** — Exchange several NOSTR messages incl. a file, kill and reopen the app → all messages still present with correct direction, status, and reply linkage.  _(bug_campaign `v2.11.0`)_
- [ ] **Self-address drift — own UA variants not recognized as self (#205)** — JVM: :ui-lib:testZcashmainnetFossDebugUnitTest self-address registry tests — isSelfAddress true for both canonical and diversified UA of the same wallet. Device: fresh wallet, verify sent messages never render as incoming and group creator…  _(canonical_addr_and_swap_batch)_
- [ ] **NOSTR timestamp remote-DoS kills inbound collector** — Unit: feed a gift-wrap event with created_at = Long.MAX_VALUE and a negative value through the inbound parse path; assert no DateTimeException and that subsequent events are still collected. Source check: both Instant.ofEpochSecond sites a…  _(deep_audit_jul9 `39264ff28`)_
- [ ] **#8: inbound message stranded when no collector alive** — Unit: deliver an inbound message with no collector attached -> assert pendingRedelivery flag set; attach a live collector and tick the watchdog -> assert exactly ONE dedup-clearing redeliverBacklog() call and the flag consumed (no per-tick…  _(deep_audit_jul9 `159b0fc40`)_
- [ ] **BUG#2: wallet sync silently wedges while foregrounded** — Device: keep ZCHAT foregrounded on Honor for 30+ minutes, then send an on-chain message from Seeker; it must arrive WITHOUT restarting Honor. Confirm sync-liveness watchdog kick lines appear in logcat if sync stalls. Regression smell: any …  _(deeptest_bugs4_5 `e48e91249`)_
- [ ] **BUG#3: NOSTR inbox goes stale while backgrounded** — Device: background ZCHAT on Seeker for 15+ minutes (do NOT force-stop), send a Tunnel message from Honor, then re-foreground Seeker — the message must appear without force-stopping. Note from the campaign: 'reaction/request not received' w…  _(deeptest_bugs4_5 `490a6fef4`)_
- [ ] **First-contact sender never sent KEX — recipient saw 'Unknown sender, cannot reply'**  💵 — Two-device (Honor->Seeker): on the sender, clear E2E state for a brand-new peer address (or use a fresh identity), send a first VAULT/on-chain message. On the recipient after sync, the conversation must show an identified, replyable sender…  _(first_contact_kex_fix)_
- [ ] **Recipient dropped first-contact KEX (unknown conversation ID / no address on wire)**  💵 — Unit: parseKEXWire with senderAddress=null must recover the appended u1 address (len>60, startsWith u1) from the payload and verify the signature against it; ParsedKEX.senderAddress populated. Device: clear the receiver's convId->peer mapp…  _(first_contact_kex_fix)_
- [ ] **Time-lock message lands in dead 'Unknown sender' chat** — 2-device: send a time-locked message from A to B in VAULT -> on B it must appear inside the existing thread for A (and any previously-split 'unknown' chat content merges back), never in a chat named 'unknown'. Validated originally by the u…  _(messaging_recovery_batch `797fe396b`)_
- [ ] **Mode desync between peers (A TUNNEL / B OPEN, asymmetric E2E)**  💵 — 2-device (Honor+Seeker): set A to TUNNEL and B to OPEN with E2E toggled asymmetrically; send messages both directions -> both sides must converge to a working transport and decrypt both ways, without either user manually matching settings.…  _(mode_flow_bug_batch `e3f111ab4 + 37a2e5cb3 (B1/B2)`)_
- [ ] **Raw E2E1 envelope rendered as a chat bubble after Reset Encryption** — 2-device: run Reset Encryption on device A while B has stale keys, have A send during/after the reset window -> B must NEVER render a literal E2E1:/ZBOOT/ZREQ_FULFILL/KEX string as a chat bubble (general rule from this batch: no raw protoc…  _(mode_flow_bug_batch `61cf8f661 (B4)`)_
- [ ] **'Reconnect (resend my key)' button does nothing**  💵 — 2-device: tap 'Reconnect (resend my key)' on device A -> observe an actual KEX/key resend (logcat on A shows the send; B receives/adopts the key) and messaging works afterward; repeat from B.  _(mode_flow_bug_batch `1479bbe75 (B5)`)_
- [ ] **#257 durable KEXACK stamp in chat-list flow map corrupts reset recovery**  💵 — Code check: chat-list flow map must be display-only (no persisted KEXACK stamp written from the chat-list path). Behavioral: run a full Reset Encryption cycle on 2 devices while the chat list is visible -> recovery completes exactly as in …  _(mode_flow_bug_batch `0c6104696 (#257)`)_
- [ ] **No verified-vs-encrypted trust state for KEX TOFU (first-contact MITM window)**  💵 — On Honor (debug build): open an established chat -> safety-number/verification dialog -> tap 'Mark as verified' -> verified checkmark shown and survives app restart; then have the peer Reset Encryption (forcing re-KEX/key change) -> our si…  _(orchard_vuln_audit)_
- [ ] **clearE2EKeys left stale verified/key-changed trust flags**  💵 — Code check: clearE2EKeys implementation removes both e2e_verified_ and e2e_key_changed_ prefs for the peer. Device: mark a chat verified -> Reset Encryption on that chat -> reopen the safety-number dialog: it must NOT show verified for the…  _(orchard_vuln_audit)_
- [ ] **#225 NOSTR key rotation wedges all delivery from peer**  💵 — Device: rotate NOSTR identity on Seeker. On Honor expect logcat 'rotated NOSTR key — adopted (authenticated by unchanged E2E identity), chat continues' plus exactly one rotation pill. Then send Honor→Seeker AND Seeker→Honor over NOSTR — bo…  _(rotation_unread_fixes `f5afbd1b9`)_
- [ ] **Rotation-pill flood — 10,000 'contact rotated their key' pills** — Device: after any rotation, run several full sync/rebuild cycles (background+foreground the app, then restart + full sync). Assert at most 1 rotation pill per peer, 0 rotation-adopt logcat emissions on re-scan, stored pubkey stable, and me…  _(rotation_unread_fixes `f5afbd1b9`)_
- [ ] **#230 Rotating device loses ALL NOSTR send+receive until restart** — Device: on Honor rotate the NOSTR identity, then WITHOUT restarting the app send a message — logcat must show publish acks on 3/3 relays; have Seeker reply — the reply must decrypt and render on Honor. Verify peer's bootepoch advanced and …  _(rotation_unread_fixes `f5afbd1b9`)_
- [ ] **Raw ZSTAT/ZRCPT envelopes rendered as chat bubbles over NOSTR** — Device (2-device NOSTR chat): send Honor→Seeker, open the message on Seeker to trigger a read receipt/status back. Inspect both chats: NO bubble may start with 'ZSTAT/' or 'ZRCPT/'; delivery/read state must appear only as tick indicators. …  _(security_audit_2week `9da495c04`)_
- [ ] **#195 rawMemo memos overflow 512-byte limit silently (root cause of #193/#194)** — Unit: run GroupNameBoundTest (6 cases: ASCII/empty/exact/over-long/emoji/mixed) and assert an oversize rawMemo makes CreateChunkedMessageProposalUseCase throw MemoTooLongException BEFORE proposal work. Device: create a group with a >100-by…  _(security_hardening_batch)_
- [ ] **#188 NOSTR replay resurrection — dedup coupled to pending list, call signals undeduped** — Unit: hasSeenNostrEvent/markNostrEventSeen LRU bound (2000) + persistence across instance recreation. Device: deliver the same gift-wrap event id twice (relay replay, or delete the rendered message then replay the event) — the message must…  _(security_hardening_batch)_
- [ ] **#188 NOSTR rotation left inbox on old key until app restart** — Device: rotate the NOSTR identity; WITHOUT restarting the app, have the peer send a message — it must arrive and decrypt under the NEW key; confirm no 'restart the app to finish receiving' dialog appears anywhere in the rotation flow.  _(security_hardening_batch)_
- [ ] **BUG#3 NOSTR stale inbox — quiet-but-open relay never reconnects, inbound dead until app r…** — Device 2-box test: background ZCHAT on Seeker 30+ min (screen off/idle), send a text from Honor while Seeker is backgrounded, then foreground Seeker: on ProcessLifecycleOwner ON_START logcat must show NostrChatBridge.refreshInbox() -> Nost…  _(sync_wedge_watchdog `490a6fef4`)_
- [ ] **parseMemo had no ZBOOT branch — on-chain ZBOOTs routed as PLAIN with unknown sender**  💵 — Unit: feed a 'ZBOOT/v2/<convId>/…' memo string to ZMSGProtocol.parseMemo and assert the returned message carries the convId extracted via ZBootMessage.parse (not PLAIN/null). Then confirm convId→peer routing maps it to the peer whose KEX s…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **NOSTR relay subscription goes silently stale while app foregrounded (drops all inbound)** — Unit: run RelayClientTest (4 tests) in the ui-lib JVM suite. Device: keep the app foregrounded and idle past the 120s tick and confirm logcat shows 'liveness watchdog re-issued N sub(s) on M live relay(s)' (on Seeker first enable Log.d: ad…  _(tunnel_refute_and_pairing_tooling `6404d3103`)_

### 🚇 Tunnel handshake  ·  14 case(s)

- [ ] **NOSTR calls never connected — ZBOOT key exchange unimplemented end-to-end** — Two devices (Honor + Seeker), fresh contact pair: both switch the conversation to Tunnel mode; run 'adb logcat' filtered for 'Published bootstrap ZBOOT' and confirm each device publishes one; after the memos confirm on-chain, initiate a vo…  _(ai_and_calls)_
- [ ] **NOSTR subscription died silently — inbound events lost (Call bug #3)** — adb logcat -s ZCHAT_RELAY during a session: on a relay CLOSED frame the client logs it and re-subscribes within ~15s and 'EVENT in <-' continues; publish a p-tagged kind-1059 to the device npub → arrives live.  _(bug_campaign `v2.11.0`)_
- [ ] **#233 Cold Tunnel first-contact handshake deadlock between two never-paired identities** — Fresh 2-device test (requires destroying wallets — coordinate with user first): destroy+recreate both identities, fund each ~0.001 ZEC, Honor New Chat -> Tunnel -> Seeker, send first message; reopen the chat on BOTH devices over the next f…  _(tunnel_cold_first_contact_deadlock `4948deac0`)_
- [ ] **#206 Tunnel ZBOOT/KEX never fired on fresh first-contact via New Chat**  💵 — Device: create a brand-new contact, New Chat -> select Tunnel -> send first message, then confirm logcat shows 'Tunnel bootstrap: sent KEX' on chat-open WITHOUT ever using the in-chat mode switcher. Code check: AndroidChat.kt AndroidChatDe…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_
- [ ] **#208 ZBOOT single-note deadlock — retry window shorter than change maturation**  💵 — Device (needs a single-note freshly-funded wallet): run a Tunnel handshake so the KEX consumes the only note; while change matures, ZBOOT will fail; after ~10 confirmations close and reopen the chat: ensureNostrBootstrapSent must re-attemp…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_
- [ ] **#209 Responder stayed in VAULT after completing Tunnel bootstrap (asymmetric mode)**  💵 — Device 2-box: run a fresh Tunnel first-contact; on the RESPONDER after the ZBOOT arrives, verify without touching the mode picker: mode: entry in zchat_conversation_mode.xml auto-upgraded VAULT->TUNNEL, banner shows the NOSTR/E2E state (no…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_
- [ ] **Asymmetric isE2EKeyExchangeComplete guard deadlocks Tunnel bootstrap**  💵 — Code: verify ChatViewModel.ensureNostrBootstrapSent (~L548) unconditionally calls sendKEXMessage(peer, ourAddress) with no isE2EKeyExchangeComplete guard. Device: fresh Honor↔Seeker pair, initiator switches mode to Tunnel; watch logcat — t…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **False 'Secure connection established' shown when ZBOOT was silently dropped**  💵 — Code: routeIncomingBoot returns Boolean and the 'established' note is emitted only on true; false path shows 'Connection request received — finishing secure setup (waiting for key exchange)…'. Device: deliver a ZBOOT to a device that has n…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **Failed KEX send left bootstrap permanently wedged (isOwnBootSent never re-armed)**  💵 — Code: sendKEXMessage's catch calls setOwnBootSent(peer, false) (and per the later retry fix, only on permanent failure). Device: on a wallet that cannot fund the KEX, trigger Tunnel bootstrap so the KEX throws, then fund the wallet and re-…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **KEX and ZBOOT raced for the single spendable note in one bootstrap pass**  💵 — Code: ensureNostrBootstrapSent sends only the KEX in its pass (no sendMessage(boot) in the same handler); the NOSTR ZBOOT is sequenced later via sendNostrBootHandshake. Device: fresh single-note wallet, switch to Tunnel — logcat must show …  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **First-contact KEX cannot carry NOSTR pubkey — calls never unlocked after KEX-only bootstr…**  💵 — Device (2-device, fresh identities): complete first-contact handshake; both devices must gain pubkey: entries in shared_prefs/zchat_conversation_mode.xml within the 4-tx flow (KEX → KEXACK → ZBOOT_initiator → ZBOOT_responder), then a call …  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **routeIncomingBoot auto-reply gate skipped reply when stale pubkey pre-existed**  💵 — Code: routeIncomingBoot calls sendNostrBootHandshake unconditionally (the per-(peer, our-pubkey) sentnostrboot idempotency pref prevents ping-pong, catch clears it to re-arm). Device: reinstall+rescan one side of an established pair (it re…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **Handshake leg failing on transient note-lock never retried (receiver-side stall)**  💵 — Code: sendHandshakeMemoWithRetry wraps all three legs, retries only on transient insufficient-funds (message contains 'confirm on-chain', NOT 'add ZEC'), uses waitForNextBlock() observing synchronizer.networkHeight, MAX_HANDSHAKE_RETRIES=4…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **Concurrent duplicate KEXACK/ZBOOT sends (missing in-flight guards)**  💵 — Code: verify kexAckInFlight and nostrBootInFlight guards still wrap the KEXACK-send and sendNostrBootHandshake paths in ChatViewModel. Device: during a rescan that re-surfaces a peer KEX, logcat must show at most one KEXACK send per receiv…  _(tunnel_kex_bootstrap_deadlock)_

### 🆓 OPEN / free-chat  ·  1 case(s)

- [ ] **In-chat OPEN first message sent raw instead of INIT-wrapped (HIGH)** — Two-device: on the receiver, delete the sender's pubkey:<peer> line from zchat_conversation_mode.xml via run-as sed (force-stop first; survives relaunch). Sender sends an in-chat OPEN message -> receiver must show a Requests-inbox banner w…  _(free_open_msg1 `#224`)_

### 👥 Group  ·  23 case(s)

- [ ] **Group stored-message schema mismatch dropped inbound group msgs (B2)** — 2-device group: member B sends a group message → member A sees it in the group view and it persists after reopen.  _(bug_campaign `v2.11.0`)_
- [ ] **GROUP_ACCEPT never sent — joiners never got group messages (B3)** — 2-device: invite member, member accepts → inviter's roster shows ACTIVE and subsequent group messages reach the new member.  _(bug_campaign `v2.11.0`)_
- [ ] **Group dedup collapsed cross-sender same-seq messages (C4)** — JVM: dedup test with two senders sharing seq → both retained. 2-device group: both members send near-simultaneously → both messages display.  _(bug_campaign `v2.11.0`)_
- [ ] **GROUP_KICK/LEAVE address-match bypass under UA drift** — Unit: build a roster containing a member stored under one UA representation; deliver a GROUP_KICK referencing a drifted UA of the same member; assert resolvePeerAddress canonicalizes both sides, the member is marked with a LEFT tombstone, …  _(deep_audit_jul9 `41426c6b9`)_
- [ ] **#6: on-chain GROUP_MSG lacked author signature (impersonation)**  💵 — Run the ZMSGGroupProtocol unit tests: 7 resolveGroupMsgSender decision tests + protocol sig tests (pure function extracted in 460831cea). MUST verify fail-open semantics: valid sig -> authenticated; absent/unknown-key/invalid sig -> messag…  _(deep_audit_jul9 `159b0fc40`)_
- [ ] **GROUP_INVITE memo overflow (512B cap) — group invites never sent** — Unit: add/run a ZMSGGroupProtocol test asserting createGroupInviteCompact output with a real-length 213-byte inviter UA is <= 512 bytes, and that parseGroupInvitePayload tolerates missing members/enc_key. Device: on Honor, New Group inviti…  _(group_invite_compact `#194`)_
- [ ] **k2 group-key decrypt fails on inviter self-address representation drift**  💵 — Two-device: with Honor and Seeker KEX'd, create group on Honor inviting Seeker; Seeker logcat must show k2 decrypted (direct, or 'Compact invite k2 decrypted via a KEX peer' if addresses drifted) and 'Decrypted GROUP_MSG' both directions w…  _(group_invite_compact `#197`)_
- [ ] **Unauthenticated GROUP_KICK/GROUP_KEY — forgeable kick and group-key rotation**  💵 — Unit: run the group signed-control JVM tests (e.g. ./gradlew :app:testZcashmainnetFossDebugUnitTest --tests "*Group*") — an UNSIGNED or bad-signature GROUP_KICK/GROUP_KEY must be rejected/ignored (roster unchanged, no key installed), a non…  _(group_control_msg_auth `#187`)_
- [ ] **Group isCreator/admin-auth false after own address drift — admin UI hidden, key rotation …**  💵 — Unit: test that isCreator is true when isGroupSelfCreated(groupId) is set even when creatorAddress != currentUserAddress. Device: create a group on Honor ('grpcreator' flag), then after a seed-restore/reinstall (address string drifts) reop…  _(group_invite_compact)_
- [ ] **#215: GROUP_MSG decrypted with wrong epoch key — permanent '[Unable to decrypt]' after ke…**  💵 — Preferred unit test: processGroupMsg with a message encrypted at epoch 0 while device current epoch is 2 → decrypts via epoch fallback; and a message with no matching held key → distinct 'held keys, none matched' path. Device: in a group t…  _(open_mode_and_group_epoch `#215`)_
- [ ] **#214: GROUP_ACCEPT matched roster by exact address string — accept-rep drift left joiner …** — Two-device: Honor creates a fresh group and invites Seeker using a stale/different rep of Seeker's address; Seeker auto-joins and sends GROUP_ACCEPT. Honor logcat must show 'GROUP_ACCEPT: activated <addr> via E2E-key match' and the roster …  _(open_mode_and_group_epoch `d5ca43b4b`)_
- [ ] **#217: sendGroupMessage had no block-aware retry — first message after invite silently dro…**  💵 — Two-device with a single-note funded wallet: create group + invite (consuming the note), then immediately send the first group message. The send must confirm on-chain (Success txIds, checkmark in chat), with retry logs from sendGroupMemoWi…  _(open_mode_and_group_epoch `7fb155c14`)_
- [ ] **#218: Group fan-out sent to stale invited rep, not the peer's live receive address — paid…**  💵 — Two-device (fresh, never-reset wallets — old test wallets had environmental diversifier drift): invite Seeker under a stale rep, Seeker accepts; verify Honor's roster member address updates to Seeker's declared live receive address (compar…  _(open_mode_and_group_epoch `c42a8c6a7`)_
- [ ] **#218 hardening: circular address-alias pair flipped live address back to stale**  💵 — Unit test preferred: pre-seed alias(live→stale), then call setPeerAddressAlias(stale→live) — assert the canonical's own alias entry is cleared (no cycle: resolving live returns live). Device: re-trigger a group accept on a device with a kn…  _(open_mode_and_group_epoch `b7e6531a9`)_
- [ ] **#219: Unsigned GROUP_ACCEPT address-adoption forgeable — group fan-out redirectable**  💵 — Unit tests: (a) processGroupAccept with valid signature → member activated AND declared address adopted; (b) invalid/missing signature → activate-only, address NOT adopted; (c) legacy unsigned accept → still activates (no adoption). Device…  _(open_mode_and_group_epoch `b539029a7`)_
- [ ] **Group-invite remote verification-stripping primitive (regression introduced by the N1 fix)**  💵 — Code check: in ChatViewModel.processGroupInvite, a group invite whose embedded inviter key differs from the stored key for an EXISTING contact must be ignored (no key overwrite, no e2e_verified_ clear, no key-changed flag); key stored only…  _(orchard_vuln_audit)_
- [ ] **Kicked group member can keep injecting messages** — Device (2-device + group): admin (Honor) kicks member (Seeker) from a group; from Seeker send a GROUP_MSG over NOSTR; verify it does NOT render in the group chat on Honor. Unit alternative: processGroupMsg with a sender marked LEFT must be…  _(security_audit_2week `9da495c04`)_
- [ ] **Group-invite inviter spoofing via #197 k2 fallback**  💵 — Unit: an invite whose claimed inviter differs from the seal-authenticated sender must fail k2 decryption/be rejected; regression guard: a legitimate invite from a peer with drifted address representation must still be ACCEPTED. Device: sen…  _(security_audit_2week `9da495c04`)_
- [ ] **#199 Multi-member group invites silently dropped after the first**  💵 — Device (funded wallet holding a SINGLE note): create a group with 3+ members. All members must eventually receive invites (retries spanning blocks); if any invite genuinely fails, a Toast 'N invite(s) couldn't be sent — add ZEC and re-invi…  _(security_hardening_batch)_
- [ ] **#187 Unsigned GROUP_KICK/GROUP_KEY — forgeable kick / key-poison (and kick/rotate non-fun…**  💵 — Unit: run GroupControlSignatureTest (6 cases: genuine kick verifies; tampered kicked-member/epoch/key fails; wrong-key forgery fails; key rotation round-trips; canonical format stable). Device (2-device group): admin kicks a member — membe…  _(security_hardening_batch)_
- [ ] **NOSTR GROUP_INVITE arriving before address load was permanently lost** — Unit: run the full ui-lib JVM suite (was 458/458 green with this fix). Code: pendingGroupInvites queue keyed by groupId with drain in loadConversations, plus unmarkNostrEventSeen() on defer and re-mark on join/give-up, gift-wrap eventId th…  _(tunnel_refute_and_pairing_tooling `f483dd85d`)_
- [ ] **#215 unbounded group-epoch decrypt loop (no exception guard)** — Unit: assert MAX_GROUP_EPOCH_LOOKBACK == 64 exists and is enforced — feed ChatViewModel.processGroupMsg and GroupViewModel.parseAndDecryptGroupMessage a GROUP_MSG whose epoch is >64 behind currentEpoch and assert at most 64 candidate epoch…  _(v2_11_ship_and_criticize `f560245dc (v2.11.0)`)_
- [ ] **#223 group pending/mined reconcile by seq-only hides messages** — Unit: reconcile test with two senders whose messages share overlapping seq values — sender B's mined message with seq N must NOT clear/suppress sender A's pending message with seq N; match must require (sender, seq, epoch) equality; assert…  _(v2_11_ship_and_criticize `f560245dc (v2.11.0)`)_

### 🚪 Onboarding / restore  ·  3 case(s)

- [ ] **Re-tapping 'Create wallet' overwrote the seed — orphaned funds (#189 #2b)**  💵 — JVM: call createNewWalletForOnboarding() twice → the persisted PersistableWallet after the second call is IDENTICAL to the first (early-return). Device: kill the app during onboarding, relaunch, tap Create again → receive address matches t…  _(canonical_addr_and_swap_batch)_
- [ ] **ONB-4: Back during onboarding re-mounted one-shot step, re-running wallet creation / PIN …**  💵 — USER-GATED (needs wipe → fresh onboard; was deferred on-device): during fresh onboarding, after the create-wallet step advances, press Android Back — the step must NOT re-mount and logcat must NOT show a second createNewWalletForOnboarding…  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **Onboarding free-first silently broken — invite code missing NOSTR key**  💵 — Device: wipe + fresh onboard a test device (mint path). Immediately after onboarding completes, open the invite/contact code and verify it CONTAINS the &n= NOSTR pubkey parameter. End-to-end: a second fresh device using that code must get …  _(security_audit_2week `9da495c04`)_

### 🛠 Backend / infra  ·  1 case(s)

- [ ] **Compromised ADMIN_SECRET rotated** — curl the backend admin endpoint with the OLD secret b5a0…ecff → expect 401; with the current secret from apps/backend/.env → expect 200. Verify .gitignore contains '.env.*'.  _(audit_pending_items)_

### 🔁 Swap  ·  4 case(s)

- [ ] **Swap div-by-zero on zero exchange rate crashed status poll (#198 C1)**  💵 — JVM: construct SwapQuote/SwapQuoteStatus with amountInFormatted=0 → zecExchangeRate/affiliateFeeZatoshi return guarded values, no exception; exercise the status-poll mapper with a zero-amount payload.  _(canonical_addr_and_swap_batch)_
- [ ] **Swap FIAT-mode crash on null/zero usdPrice (#198 H1)**  💵 — JVM: ExactInputVMMapper helpers with usdPrice=null and usdPrice=0 → no crash, graceful null/absent conversion.  _(canonical_addr_and_swap_batch)_
- [ ] **Unbounded swap slippage allowed (#198 L2)**  💵 — Enter slippage 51%+ on the slippage screen → Confirm stays disabled; 50% or below enables it.  _(canonical_addr_and_swap_batch)_
- [ ] **Stale swap quote race — prior quote could bind (#198 M1)**  💵 — JVM/integration: fire two quote requests back-to-back with the first resolving late → the observer never receives the first request's Success; state goes Loading synchronously on the second request.  _(canonical_addr_and_swap_batch)_

### 🖼 UI / UX / nav  ·  2 case(s)

- [ ] **Destroy-PIN changeable from Settings with zero auth** — Device (Honor, destroy-PIN ***REMOVED*** already set): Settings → change destroy-PIN. A device-credential prompt (device PIN ***REMOVED***) MUST appear BEFORE the change screen; cancelling the prompt must abort the change.  _(security_audit_2week `9da495c04`)_
- [ ] **FLAG_SECURE missing on Destroy-PIN setup and app-access auth screens** — Instrumented: run SensitiveScreenInvariantTest (2 tests) on the Honor device (***REMOVED***) — asserts FLAG_SECURE on DestroyPinSetupView and AppAccessAuthentication; also ScreenSecurityTest (3). Manual device check: adb screencap on th…  _(v2_11_0_session `v2.11.0`)_

---

## TIER 2 — High (run every release)

### 🖼 UI / UX / nav  ·  62 case(s)

- [ ] **Reset Confirm blocked when optional metadata-backup checkbox unchecked** — On device: open the reset flow, uncheck 'Keep encrypted metadata backup', verify Confirm remains enabled and the reset proceeds.  _(fb:uiux_audit_cycles)_
- [ ] **Nickname Save disabled on blank broke clear-to-delete** — Unit: run JVM test nickname_clearBySettingBlank in :ui-lib:testZcashmainnetFossDebugUnitTest — must pass. Device: set a nickname, reopen, clear the field to blank, tap Save (must be enabled), verify nickname is deleted.  _(fb:uiux_audit_cycles)_
- [ ] **InsufficientFunds OK one-shot latch could trap user** — On device: attempt a Send with amount greater than balance to trigger the InsufficientFunds dialog; tap OK — dialog must dismiss and navigation complete even if slow; re-trigger the dialog and confirm OK still works the second time.  _(fb:uiux_audit_cycles)_
- [ ] **Contact-name trim applied inconsistently across contact ViewModels** — On device: enter a name with leading/trailing spaces (e.g. '  Alice  ') in every contact-name entry point (new contact, edit contact, and the third contact VM's screen, e.g. request-accept naming); save each; verify all three store the ide…  _(fb:uiux_audit_cycles)_
- [ ] **AI-tab open froze main thread ~240ms (StrictMode DiskReadViolation)** — On device (debug build): run 'adb logcat -s StrictMode' while cold-starting the app and opening the AI tab twice; the second open must show 0 DiskReadViolations with AiPreferences/AiImageStore frames and no visible freeze (first open may s…  _(fb:uiux_audit_cycles)_
- [ ] **AI tab content vanished when switching Chat/Image mode** — On device: AI tab — send a chat prompt and get a reply; switch to Image mode and generate an image; switch back to Chat — the reply must still be shown; switch to Image — the image must still be shown; tap Clear and verify only the current…  _(ai_and_calls)_
- [ ] **Image generation hardcoded to removed model flux-1-schnell** — On device: AI tab Image mode — verify the model picker lists only priced image models (flux-1-schnell absent), select venice-sd35, generate, and confirm an image renders end-to-end (validated on Honor previously). If the API returns no ima…  _(ai_and_calls)_
- [ ] **Outgoing ZBOOT rendered as raw 'ZBOOT/v1/...' memo bubble** — On device with a fresh peer: trigger a ZBOOT send (switch conversation to Tunnel/Open or start a call without keys); verify the chat shows 'Secure connection request sent' with the lock emoji and NO raw 'ZBOOT/v1/' text, both immediately (…  _(ai_and_calls)_
- [ ] **E2E banner not mode-aware** — On device: open a Vault conversation → banner says 'shielded on-chain'; open a Tunnel/Open conversation → banner says 'End-to-end encrypted over NOSTR — not on-chain'.  _(audit_pending_items)_
- [ ] **Per-message cost row shown in Open/NOSTR mode** — On device: compose in an Open-mode conversation → cost row hidden / replaced by 'Free — sent over NOSTR'.  _(audit_pending_items)_
- [ ] **Conversation top-bar icon crowding** — On device: open chat detail → Mute/Safety-number/Mode live inside the ⋮ overflow menu, not as separate top-bar icons.  _(audit_pending_items)_
- [ ] **Main-thread SharedPreferences.commit in convertToConversations** — Enable StrictMode, open the chat list with several conversations → no SharedPreferences.commit main-thread violation logged from convertToConversations.  _(audit_pending_items)_
- [ ] **More-tab routed to Advanced-Settings leaf** — On device: tap More tab → lands on the settings hub (list of sections), not Advanced Settings directly.  _(audit_pending_items)_
- [ ] **WCAG contrast + 48dp touch-target batch** — Accessibility scanner pass (or manual spot-check) on chat list, chat detail, receive, settings — text contrast passes and interactive elements are >=48dp.  _(audit_pending_items)_
- [ ] **Comm-mode not selectable at conversation start (Bug 2)** — On device: New Chat → paste valid recipient → MODE row (Vault/Tunnel/Open) appears before first send; pick Tunnel, send, reopen conversation → mode persisted. Unit: the 6 JVM + 4 androidTest mode tests stay green.  _(bug_campaign `v2.11.0`)_
- [ ] **Quantum Shield UX opaque (Bug 3)** — Unit: displayLabel() test for the three states. Device: chat detail shows 'Extra Security (Post-Quantum)' wording and the enable row with 'available after handshake' hint.  _(bug_campaign `v2.11.0`)_
- [ ] **Receive QR unscannable (Bug 6)** — On device: open My Address → QR fills ~74% of screen width with a small logo; scan it with a second device's camera → address parses.  _(bug_campaign `v2.11.0`)_
- [ ] **No Copy/Retry/progress affordances on bubbles (Bug 8b)** — Unit: the 16 JVM tests for PendingSendEstimate + recoverRawSendPayload. Device: long-press a bubble → Copy sets clipboard; a FAILED message shows Retry; a pending send shows progress/ETA.  _(bug_campaign `v2.11.0`)_
- [ ] **Sender's own NOSTR file bubble showed raw ZFILE + wrapped key** — Send an image over TUNNEL → your own bubble renders a file/image card, never raw 'ZFILE/…' text.  _(bug_campaign `v2.11.0`)_
- [ ] **Call-log entries left blank chat-list preview** — Place/receive a call → chat list preview for that conversation shows e.g. '📞 Outgoing call', not blank.  _(bug_campaign `v2.11.0`)_
- [ ] **Unread count never computed + badge stuck on stale/future timestamps (B5)** — Device: receive 3 messages → badge '3'; open the conversation, go back → badge 0; a peer message with a future timestamp must not stick the badge.  _(bug_campaign `v2.11.0`)_
- [ ] **Send-to-self allowed / wedged compose screen (B6)** — Paste your own address in New Chat → Send button disabled, screen remains usable (can edit recipient).  _(bug_campaign `v2.11.0`)_
- [ ] **Duplicate back-stack entries on repeated navigation (B10)** — Tap the same nav destination multiple times → one Back press leaves the screen.  _(bug_campaign `v2.11.0`)_
- [ ] **File download failure was silent — no retry (C2)** — Interrupt a file download (airplane mode mid-transfer) → 'Download failed — tap to retry' appears (incl. over the blur preview); tapping retry re-downloads and repaints.  _(bug_campaign `v2.11.0`)_
- [ ] **CallLogPill washed-out styling (U1)** — View a conversation with call pills → crisp filled pill with visible border and legible text in the active theme.  _(bug_campaign `v2.11.0`)_
- [ ] **Chat header subtitle hard-clipped to 'Tap to' (U2)** — Open a chat with an un-named contact → subtitle ellipsizes gracefully ('Tap to…'), never hard-clips mid-word without ellipsis.  _(bug_campaign `v2.11.0`)_
- [ ] **My-Address 'Recommended' card cut off at bottom (U3)** — Open My Address, scroll to bottom → 'Recommended' card fully visible above the system nav bar.  _(bug_campaign `v2.11.0`)_
- [ ] **Chat-list header balance wrapped to 2 lines (U4)** — With a balance having trailing zeros → header shows e.g. '0.00018' on ONE line.  _(bug_campaign `v2.11.0`)_
- [ ] **'New Message' vs 'New chat' naming inconsistency (U5)** — Check FAB label and compose-screen title → both read 'New Chat'.  _(bug_campaign `v2.11.0`)_
- [ ] **More-tab ZCHAT footer hugged bottom edge (U6)** — Open More tab → footer has visible breathing room (24dp) below it.  _(bug_campaign `v2.11.0`)_
- [ ] **Changelog hyphen/em-dash + 'Ul' typo (U8)** — Open What's New → em-dashes used consistently and 'UI' spelled correctly.  _(bug_campaign `v2.11.0`)_
- [ ] **On-chain 75s timer shown for NOSTR sends (F3)** — Send in TUNNEL → pending bubble shows 'Sending…' without a 75s countdown.  _(bug_campaign `v2.11.0`)_
- [ ] **'Extra Security' PENDING banner not clickable (R2)** — In a conversation with Extra Security pending, tap the banner → QR setup screen opens.  _(bug_campaign `v2.11.0`)_
- [ ] **E2E-over-NOSTR badge opened irrelevant on-chain panel (R3)** — In a TUNNEL chat tap the green E2E indicator → safety-number verification screen opens (not the pool panel).  _(bug_campaign `v2.11.0`)_
- [ ] **Always-on 'Extra Security: NONE' banner cluttering header (R4)** — Open a chat with Extra Security off → no banner; 'Enable' present in the ⋮ overflow menu; PENDING/ACTIVE conversations still show their banners.  _(bug_campaign `v2.11.0`)_
- [ ] **Image-send disk I/O on main thread (#203)** — With StrictMode penalties on, pick and send an image → no DiskReadViolation attributed to handlePickedImage in logcat.  _(canonical_addr_and_swap_batch)_
- [ ] **BUG#4a: received document opened in image viewer dead-end** — Device: send a .txt file Honor->Seeker; on Seeker tap the received Document bubble — the Android 'Open with' chooser must appear (e.g. HTML Viewer opens the file), never the in-app image viewer 'Cannot preview this file' dead-end.  _(deeptest_bugs4_5 `69e7ff8cd`)_
- [ ] **Cryptic undecryptable-message placeholder after seed restore** — Unit/source: verify the catch branch of ChatViewModel.tryDecryptMessage (decrypt-failure fallback) produces the explanatory 'decryption key isn't on this device... Ask the sender to resend' text, not the old generic placeholder, and that i…  _(e2e_key_loss_on_restore `126e27618`)_
- [ ] **Waiting-for-reply banner hidden on Tunnel chat mid-ZBOOT handshake** — Device: start a fresh Tunnel chat (peer has not yet replied / ZBOOT pending). Chat-detail must show BOTH the muted call buttons AND the header 'Waiting for your contact to reply' banner simultaneously; after the peer replies and isE2EReady…  _(fable5_ux)_
- [ ] **Call buttons dead with no explanation before E2E ready** — Device: in a fresh Tunnel chat before the contact replies, verify call buttons render with muted tint and tapping one shows the 'Calls unlock after your contact replies' Toast; after the contact replies, buttons work normally.  _(fable5_ux)_
- [ ] **No warning when switching a chat to Open mode (privacy footgun)** — Device: New Chat -> select Open mode for the first time in that screen -> warning dialog appears; select away and re-select in the same visit -> no repeat; leave and re-enter the screen -> warns again. Open button subtitle states the reduc…  _(fable5_ux)_
- [ ] **Mode picker buttons opaque — no plain-language tags** — Device: open the New Chat mode picker -> all three mode buttons show their plain-language tag regardless of which is selected (Vault=On-chain, Tunnel=Fast & free, Open=Any wallet).  _(fable5_ux)_
- [ ] **New-chat compose defaulted to VAULT (paid on-chain per message) instead of TUNNEL** — Device: New Chat -> enter a fresh recipient address with no stored mode -> Tunnel is highlighted by default and the picker remains visible; a peer WITH a stored per-peer mode still opens with that stored mode. Unit: getConversationModeOrNu…  _(fable5_ux)_
- [ ] **Keyboard covers dialog buttons in special-feature dialogs** — On device: open each of the 3 special-feature dialogs (Block / Payment / Secret selector) from chat, focus a text field so the keyboard opens -> action buttons remain reachable (dialog body scrolls above the IME).  _(messaging_recovery_batch `797fe396b`)_
- [ ] **Time-lock countdown frozen/jumpy** — On device: receive a time-locked message with a short lock -> countdown visibly ticks every second; stay in the chat through unlock time -> content reveals live without leaving/re-entering the chat.  _(messaging_recovery_batch `46068807d (+ 0cef70d32 live reveal)`)_
- [ ] **Time-lock composable outside Vault then rejected (compose-then-reject)** — On device: in an OPEN or TUNNEL chat open the + / special-features menu -> the time-lock entry is greyed out with the 'Vault chats only' subtitle and cannot open the composer; switch to VAULT -> entry becomes active.  _(messaging_recovery_batch `46068807d + 0cef70d32`)_
- [ ] **Chat header truncation / cramming** — On device: open a chat with a long contact name -> header shows voice/video/overflow buttons, the E2E toggle lives inside the overflow menu, and the name renders with the wider space (compare no worse than the 759px-equivalent layout).  _(messaging_recovery_batch `c624f0b48`)_
- [ ] **VAULT send-countdown restarts from 0 on chat re-enter** — On device: start a VAULT send so the countdown begins, back out of the chat, re-enter -> countdown continues from the correct remaining time, not from 0.  _(mode_flow_bug_batch `d226f1074 (B3)`)_
- [ ] **In-chat search: no jump-to-first and wrong '4 results' cap** — On device: in a chat with 6+ occurrences of a word, run in-chat search -> the reported match count equals the real count (not capped at 4) and search jumps to the first match.  _(mode_flow_bug_batch `a650c7612 (B9/B10)`)_
- [ ] **See-all header color wrong + theme color inconsistency** — On device: cycle all 4 themes (Default/Light/Dark/Nightwire) and check the See-all header (and the swept screens) render in theme-correct colors - no stale colors from another theme.  _(mode_flow_bug_batch `720a7de14 (B13/B14)`)_
- [ ] **Call mute button inactive during ringing** — 2-device: place a voice call from A to B; while it is still ringing (pre-answer) tap mute on the caller -> control is active and toggles mute state.  _(mode_flow_bug_batch `266355467 (B15)`)_
- [ ] **NAV-1: Bottom-nav tabs entered via forward() strand stale back-stack entries** — On device: tap tabs AI → Wallet → AI → Chats, then press Back once — app must go to root/exit, NOT show a stale Wallet screen. Repeat with several tab orderings; Back must never resurface a previously-visited tab.  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **NAV-2: Rapid double-Back swallowed by navigateWithBackoff de-dup** — Navigate two levels deep (e.g., More → a sub-screen → deeper screen), press Back twice within 0.5s — both pops must execute (land two levels up). Unit alternative: NavigationRouter test asserting two rapid Back commands both dispatch while…  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **MORE-1: Reset Zashi showed false 'Success' with dead OK on biometric-cancel** — USER-GATED (touches wipe flow; was deferred on-device): More → Reset Zashi → when the biometric prompt appears, cancel it — expect NO 'Success' dialog and return to a live screen. Complete a real reset separately and confirm the success OK…  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **Avatar broadcast lost to process death — profile photo never propagated to contacts** — 2-device debug builds (confirm BOTH devices run the build under test — dumpsys versionName + dex-grep 'ZPROF' first): on Honor set a new profile photo via the picker, let Honor background-kill the app, relaunch -> adb logcat -d / grep ZPRO…  _(release_build_requirements `b68252c44 (v2.12.1), 557a5f85f (v2.12.2)`)_
- [ ] **#226 Unread badge never clears after opening chat** — Device: with Honor on the chat list, send a message from Seeker so the unread '1' badge appears. Open that chat on Honor, press back to the list. Badge must be cleared immediately, no app restart.  _(rotation_unread_fixes `f5afbd1b9`)_
- [ ] **Stale Destroy-PIN copy** — Device: open the destroy-PIN setup/settings screen on Honor and read the copy — it must accurately describe current behavior (device-credential-gated change, wipe semantics). No stale text.  _(security_audit_2week `9da495c04`)_
- [ ] **#190 Safety-number verify dialog silent on key change** — Device: force a key-changed state for a peer (e.g. peer seed-restore/re-KEX so isKeyChanged flags), then open the verify/safety-number dialog — a red key-CHANGED warning must appear at the top instructing to re-compare the new number.  _(security_hardening_batch)_
- [ ] **Established Tunnel showed on-chain cost row despite sends being free over NOSTR** — Device: open an established Tunnel chat (peer pubkey known, NOSTR publisher live): input bar must show 'Free — sent over NOSTR' with NO 'Message cost' row; on a Tunnel whose handshake is still pending, the cost row must still appear. Code …  _(tunnel_cold_first_contact_deadlock `bb193b65b`)_
- [ ] **#207 Post-send blank dark screen — backToRoot() pops the nav root** — Device: perform a wallet send -> tap Close on the 'Sent!' screen: app must land on the home screen (not a blank dark screen), and BACK from there behaves normally. Code check: Navigator.kt backToRoot uses `while(previousBackStackEntry != n…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_
- [ ] **Call buttons swallowed tap when E2E not ready, blocking recovery** — Device (chat-detail is FLAG_SECURE — drive by uiautomator label, not screencap): open a chat where E2E is not ready (muted call icon), tap Call; logcat must show startCall reaching onNeedKeyExchange → ensureNostrBootstrapSent(force=true) p…  _(tunnel_kex_bootstrap_deadlock)_
- [ ] **Decrypt failure rendered raw 'E2E1:' ciphertext blob in chat** — Device: create a ratchet desync (or inject an undecryptable E2E1: memo) and verify the bubble shows the lock-emoji 'Encrypted message' placeholder, never a raw 'E2E1:' string. Code: convertToConversations replaces the blob on decrypt failu…  _(v2_11_0_session `v2.11.0`)_

### 🔁 Swap  ·  4 case(s)

- [ ] **Swap slippage screen crash on malformed fiatAmount arg (#198 L1)**  💵 — JVM: SwapSlippageVM with a non-numeric fiatAmount arg → no NumberFormatException, screen state builds.  _(canonical_addr_and_swap_batch)_
- [ ] **Unknown swap mode crashed quote sheet title (#198 L3)**  💵 — JVM: SwapQuoteVMMapper title with a non-exact mode value → returns the generic title, no exception.  _(canonical_addr_and_swap_batch)_
- [ ] **SWAP-1: SwapQuote blank sheet with dead Back when quote is permanently null** — On device: navigate into the Swap quote screen in a state where no quote exists (e.g., enter quote flow without completing amount/asset) — screen must auto-back to Swap instead of showing a blank sheet; if any sheet shows, one Back press m…  _(nav_deadbutton_class `10b3d357c`)_
- [ ] **SWAP-2: SwapAssetPicker dead-Back window on cold open (transient null state)** — adb shell am force-stop the app, relaunch, go straight to Swap → asset picker and press Back immediately during the initial load window — must dismiss back to Swap. Then re-open picker warm with state loaded and press Back once — must land…  _(nav_deadbutton_class `10b3d357c`)_

### 👥 Group  ·  5 case(s)

- [ ] **Group 'Tap to sync keys' recovery UI unreachable dead code** — Code check: grep ui-lib chat module for 'Tap to sync keys'; if the string exists, trace the group decrypt-failure path and confirm a message object carrying the recovery affordance is actually constructed on decrypt-null (i.e. the UI is re…  _(fb:uiux_audit_cycles)_
- [ ] **Group-member picker missing KEX'd conversation peers** — Device: on Honor with an active KEX'd chat to the Seeker peer whose address is NOT saved in the Address Book/contact book, open New Group -> member picker: the Seeker peer (u1rumw34...) must be listed with its display name and selectable. …  _(group_invite_compact `#196`)_
- [ ] **Signed kick/rotate (#187) had no UI trigger — admin couldn't kick or rotate from Group Se…** — Device: on Honor, create a group with the Seeker member ('AdminTest' pattern) -> open Group Settings as creator: per-member Remove (X) and 'Rotate Group Key' buttons must be visible; tap Remove -> confirm -> roster count decreases (2->1) a…  _(group_invite_compact `#204`)_
- [ ] **Group-invite log leaked address prefix** — grep the group-invite handling in ChatViewModel.kt: the address logging must go through redactAddress() with no raw .take(16) on an address; on-device, process a group invite and confirm logcat (plain 'logcat -d / grep' — never trust -s fi…  _(orchard_vuln_audit)_
- [ ] **Kicked group member still shown in roster ('deleted but still in group')** — Device (2-device): create a group on Honor, add Seeker, verify count=2; kick Seeker — roster must hide the member and count go 2→1; the add-member picker must exclude Seeker while in-group and re-include it after removal; ZChat Team must n…  _(tunnel_refute_and_pairing_tooling `70f7f356b`)_

### ✉️ Messaging delivery  ·  21 case(s)

- [ ] **Send gated on non-zero amount broke 0-ZEC free send** — On device: open a chat, open the MessageAmount selector, choose '0 ZEC — Free (may be delayed by miners)', type a message, verify Send is enabled and the message sends successfully.  _(fb:uiux_audit_cycles)_
- [ ] **'Wait for previous msg on-chain' threw instead of queueing (Bug 8a)** — On device with one spendable note: send two on-chain messages back-to-back → second shows queued/progress (no error toast), auto-sends after the next block (~75s).  _(bug_campaign `v2.11.0`)_
- [ ] **Queue-retry timeout failed the wrong message (removeAt(0))** — JVM test: queue two pending sends, time out the second → only its pendingId is removed and FAILED, the first stays queued.  _(bug_campaign `v2.11.0`)_
- [ ] **recoverRawSendPayload swallowed user messages starting with 🔐** — JVM test: recoverRawSendPayload on a message starting with '🔐 hello' → returned retryable; on '🔐 Secure connection request…' → treated as placeholder.  _(bug_campaign `v2.11.0`)_
- [ ] **Delete was a no-op for NOSTR/local messages** — Delete a NOSTR message and a call-log pill → both disappear and STAY gone after killing and reopening the app.  _(bug_campaign `v2.11.0`)_
- [ ] **Incoming reactions never displayed** — 2-device: peer reacts 👍 to your message → the emoji appears on that bubble on your device.  _(bug_campaign `v2.11.0`)_
- [ ] **View-once over NOSTR never flipped to Viewed** — Receive a view-once file over TUNNEL, open it → the row immediately shows Viewed and cannot be reopened, without restarting the app.  _(bug_campaign `v2.11.0`)_
- [ ] **Replies carried no on-chain linkage (B4a)** — JVM: untagReply round-trip tests (incl. nostr-/pending- ids). 2-device: reply to a message → receiver's bubble shows it as a reply with replyToId resolved.  _(bug_campaign `v2.11.0`)_
- [ ] **Reply quote missing cross-device over NOSTR (R1)** — 2-device TUNNEL: A replies to B's message → B sees the reply WITH the quoted preview text. JVM: tagReply/untagReply/escapeReplyPreview round-trip tests.  _(bug_campaign `v2.11.0`)_
- [ ] **Image send blocked by 'wait for ZEC' in VAULT mode**  💵 — 2-device with established pair (peer NOSTR key known): switch chat to VAULT with near-zero spendable ZEC on sender, send a photo -> image delivers without 'wait for ZEC' and without an on-chain send; any error shown must be photo/file-spec…  _(messaging_recovery_batch `797fe396b (+ 0cef70d32 error polish)`)_
- [ ] **Message order jump when pending message confirms** — 2-device: send several messages in quick succession in VAULT (on-chain), note each bubble's position while pending, wait for confirmations -> no message changes position in the thread when it confirms.  _(messaging_recovery_batch `c624f0b48`)_
- [ ] **Time-lock send hard-fails on note maturation ('why wait for blockchain')** — On a low-balance single-note wallet: send an on-chain message, then immediately send a time-locked message while the change note is immature -> it must queue and auto-retry per block until it broadcasts, not hard-fail. Verify the 4 senders…  _(messaging_recovery_batch `c624f0b48`)_
- [ ] **Decrypt-failure recovery undiscoverable (no Re-establish banner)** — 2-device: induce ratchet desync so inbound messages hit AEADBadTagException -> the affected chat must show the 'Re-establish' banner inline, and tapping it starts the reset/re-KEX recovery flow.  _(messaging_recovery_batch `0cef70d32`)_
- [ ] **Inbound partner registration gap** — 2-device: from a device unknown to the receiver, send a first-contact message -> receiver must register the sender as a partner/contact (thread routable, not orphaned). Cross-check exact expected behavior against /home/yourt/zchat-android/…  _(messaging_recovery_batch `0cef70d32`)_
- [ ] **No cross-user notification on key rotation or flow change** — 2-device: on device A rotate the key (or change flow OPEN->TUNNEL->VAULT) -> device B must show an in-chat notification of the change for that conversation. Repeat for each flow-change type.  _(mode_flow_bug_batch `f5135e781 (B7)`)_
- [ ] **Message request invisible while user is in another chat** — 2-device: with receiver sitting inside an unrelated chat, send a new message request from a fresh identity -> receiver gets a global visual+audible alert; then send a request from a known peer who reset/changed -> it surfaces inside the ex…  _(mode_flow_bug_batch `f8930aa68 (B8)`)_
- [ ] **#179: Reply quote missing when replying to pending/NOSTR messages** — In an OPEN/TUNNEL chat, send a message and immediately (while it can still be pending) reply to it, and separately reply to a NOSTR-received message. On BOTH devices the reply bubble must render the quoted preview (first 50 chars) above th…  _(money_drain_and_rotation `#179`)_
- [ ] **Unbounded reaction emoji payload (DoS)** — Unit/adversarial: craft an inbound reaction payload with a >32-char emoji field — it must be rejected or truncated to 32, never rendered at full length. Device sanity: normal emoji reactions still work.  _(security_audit_2week `9da495c04`)_
- [ ] **#210 NOSTR reactions wiped on reload — recipient never rendered them** — Device 2-box over an established NOSTR chat: react with an emoji to a peer message, then force-stop + relaunch BOTH devices and reopen the chat: the reaction must still render on both sides. Code check: ZchatPreferences.addNostrReaction/ge…  _(tunnel_handshake_e2e_validated `ea2ebeaaa`)_
- [ ] **#211 File download ran on main thread — StrictMode DiskWriteViolation on every image rece…** — Device 2-box: send an image over an established Tunnel; on the RECEIVING device watch logcat for StrictMode DiskWriteViolation mentioning FileDownloadCache during download/decrypt/render — must be zero hits while the image downloads and re…  _(tunnel_handshake_e2e_validated `fd4a3c2ef`)_
- [ ] **#203 Image upload prep ran on main thread — StrictMode disk-write on send** — Device: send an image from Honor over an established Tunnel; watch the SENDER's logcat during ImageUploadPrep -> Blossom upload -> ZFILE memo for StrictMode DiskWriteViolation hits — must be zero, and the image must upload and render on th…  _(tunnel_handshake_e2e_validated `d247dd9f9`)_

### 🚇 Tunnel handshake  ·  16 case(s)

- [ ] **Calls need ZBOOT round-trip — NOSTR keys not in KEX (Bug 4)** — Unit: the 5 androidTest KEX tests (incl. backward-compat legacy KEX and tampered-field). 2-device: create a FRESH conversation → call connects without waiting for ZBOOT.  _(bug_campaign `v2.11.0`)_
- [ ] **'Call subsystem not ready' race (Bug 9)** — On device: cold-start the app, immediately open a NOSTR-capable conversation and tap Call → call proceeds (no not-ready error) within ~5s.  _(bug_campaign `v2.11.0`)_
- [ ] **One-way ZBOOT bootstrap permanently blocked calls (Call bug #1)** — On a conversation whose peer npub is missing (grep modePrefs nostr empty): tap Call → logcat shows 'Published bootstrap ZBOOT to <peer>' instead of silence.  _(bug_campaign `v2.11.0`)_
- [ ] **ZBOOT handshake was one-way — receiver never replied (Call bug #2)** — 2-device (both on current APK), fresh ZBOOT-path pair: device A publishes ZBOOT → device B auto-replies → BOTH devices hold the peer npub after one round-trip and a call can be placed either direction.  _(bug_campaign `v2.11.0`)_
- [ ] **Phantom incoming-call screen from relay backlog replay** — With stale call signals on the relay (or after >2min-old RING), kill+reopen the app → NO incoming-call screen; logcat shows 'ignoring STALE ZCALL … age=…>120s'.  _(bug_campaign `v2.11.0`)_
- [ ] **FGS microphone type never set — mic blocked on backgrounded callee** — During an active call: `adb shell dumpsys activity services / grep -i foreground` → foregroundServiceType includes microphone (raw=129/0x81); logcat shows 'FGS type -> call(mic)'.  _(bug_campaign `v2.11.0`)_
- [ ] **No TURN server — cross-network calls failed at ICE** — Place a call: logcat shows 'local ICE candidate typ=relay' (>=1); verify coturn is up on relay.zsend.xyz:3478 and allocations succeed from both devices.  _(bug_campaign `v2.11.0`)_
- [ ] **Directional ICE failure — caller candidates dropped while callee ringing** — 2-device: place calls in BOTH directions → ICE goes CHECKING→CONNECTED within ~1-2s each way, stable two-way audio.  _(bug_campaign `v2.11.0`)_
- [ ] **Notification call-Accept could not open app (real-call bug A)** — Background the app, receive a call, tap Accept on the notification → app opens directly into the connected call.  _(bug_campaign `v2.11.0`)_
- [ ] **Mute toggle laggy/needed 2 taps on Bluetooth (real-call bug B)** — During a BT-headset call tap mute once → icon flips instantly and audio mutes.  _(bug_campaign `v2.11.0`)_
- [ ] **Bluetooth audio dead at call start (real-call bug C)** — Connect a BT headset BEFORE placing a call → audio routes to the headset from call start.  _(bug_campaign `v2.11.0`)_
- [ ] **Call lingered ~10s after peer hung up (real-call bug D)** — 2-device: callee backgrounded, caller hangs up → callee call UI ends within ~8s.  _(bug_campaign `v2.11.0`)_
- [ ] **Rapid call→hangup→call rejected for ~2s** — 2-device: call, hang up, immediately call again → second call dials without delay.  _(bug_campaign `v2.11.0`)_
- [ ] **Cold-start call accept dropped (B9)** — Kill the app; receive a call; tap Accept on the notification → app cold-starts and answers the (still-ringing) call.  _(bug_campaign `v2.11.0`)_
- [ ] **Call signals stored indefinitely on relays (NIP-40 TTL)** — Place a call, then query the relay: ZCALL kind-1059 events carry expires_at (~600s ahead); chat DM events have NO expiration tag.  _(bug_campaign `v2.11.0`)_
- [ ] **Calls gated on message mode instead of call channel (VAULT hid free-call buttons)** — Device: put an established pair (pubkey: entry present in shared_prefs/zchat_conversation_mode.xml via 'adb shell run-as xyz.zsend.zchat.foss.debug cat shared_prefs/zchat_conversation_mode.xml') into VAULT mode; call buttons must be visibl…  _(tunnel_kex_bootstrap_deadlock)_

### 🆓 OPEN / free-chat  ·  5 case(s)

- [ ] **Accepting a message request on key-change dead-ended the UI (HIGH)** — Unit: call ChatViewModel.acceptMessageRequest for a request whose pubkey differs from the stored pubkey for the same claimed address -> returns the explicit key-change AcceptRequestResult (not silent success/no-op). Device: engineer the sa…  _(free_open_msg1 `#224`)_
- [ ] **Message-request rumorId correlation defect (LOW)** — Unit: dispatch the same INIT twice (re-wrapped with different outer gift-wrap eventIds but the same rumor) into NostrChatBridge -> exactly ONE MessageRequest entry persists in zchat_message_requests_encrypted store; seen-event marking prev…  _(free_open_msg1 `#224`)_
- [ ] **Self-spoof TOCTOU at message-request accept gate (LOW)** — Unit: construct a MessageRequest whose claimed address/pubkey matches the local identity, persist it directly into the request store (bypassing routing), then call acceptMessageRequest -> must be rejected by the self-spoof guard.  _(free_open_msg1 `#224`)_
- [ ] **Message-request freshness check not symmetric (LOW)** — Unit: feed INIT events timestamped far in the PAST and far in the FUTURE into the request routing path -> BOTH rejected; an in-window event is accepted.  _(free_open_msg1 `#224`)_
- [ ] **New chat defaulted to TUNNEL even when free OPEN was possible (known peer NOSTR key)** — Device (Seeker): add a contact via scanned/pasted `zchat:` contact code (NOSTR key known), open New Chat to that peer: mode must pre-select 'Open — Free / NOSTR' with the picker still visible. Counter-cases: New Chat to an address-only pee…  _(tunnel_cold_first_contact_deadlock `bb193b65b`)_

### 🔐 Crypto / E2E / ratchet  ·  6 case(s)

- [ ] **MED-B: false 'key changed' banner from ZBOOT dedup eviction**  💵 — Unit: churn 500+ distinct ZBOOT sigs through the dedicated `zchat_boot_seen` 500-LRU (hasProcessedBootSig/markBootSigProcessed); assert KEX/KEXACK txids in the shared FIFO are NOT evicted and re-delivering a previously-seen KEX raises no k…  _(deep_audit_jul9 `159b0fc40`)_
- [ ] **#181: Quantum Shield regenerated secret on re-open, invalidating peer's already-scanned QR**  💵 — On Honor: open Quantum Shield dialog for a peer, close, re-open — logcat must show 're-using existing secret' and the QR payload must be byte-identical across opens. After PSK is established, a repeat scan on the peer must be a no-op (stat…  _(money_drain_and_rotation `#181`)_
- [ ] **Replay seen-counter set grew unbounded**  💵 — Code: E2ERatchet replay sets are in-memory session-scoped (not persisted) with pruning; constants MAX_SKIP=1000 / MAX_SEND_COUNTER=1_000_000 intact. Unit: E2ERatchetTest skip-backfill, max-skip-boundary, max-skip-rejected pass.  _(v2_11_0_session `v2.11.0`)_
- [ ] **InMemoryRatchetStateStore.mutexFor not thread-safe**  💵 — Code: InMemoryRatchetStateStore.mutexFor is synchronized. Unit: full E2ERatchetTest suite green (it exercises this store).  _(v2_11_0_session `v2.11.0`)_
- [ ] **GCM nonce missing direction byte (defense-in-depth)**  💵 — Unit: E2ERatchetTest 'A2B/B2A distinction' and 'bidirectional' cases pass. Code: nonce construction still sets nonce[0] = direction.  _(v2_11_0_session `v2.11.0`)_
- [ ] **Quantum Shield scanner callback never set — PSK scan dead**  💵 — Unit: QuantumShieldStateTest (5 tests) green. Device (2-device): initiate Quantum Shield in a chat, display the QR on one device, scan from the other — the banner must transition NONE → PENDING → ACTIVE (purple), proving the bridge callbac…  _(v2_11_0_session `v2.11.0`)_

### 💰 Money / on-chain send  ·  2 case(s)

- [ ] **Wallet screen shows 0.000 while chat list shows real balance**  💵 — On a wallet with nonzero balance: compare the chat-list balance and the Wallet screen balance -> identical (Wallet never 0.000 when chat list shows funds); tap the chat-list balance -> navigates to Wallet.  _(mode_flow_bug_batch `6ecf0d376 (B11/B12)`)_
- [ ] **Received-but-confirming funds showed 'add ZEC' instead of 'confirming'**  💵 — Device: send ZEC to the device, and before ~10 confirmations attempt an on-chain chat op — the UI message must say funds are confirming on-chain, not 'add ZEC'. Code: pendingShieldedBalance check present in AndroidChat and GroupViewModel w…  _(tunnel_kex_bootstrap_deadlock)_

### 📦 Release / distribution  ·  2 case(s)

- [ ] **Release build OOM: AGP-default R8 cannot parse Kotlin 2.3 metadata** — grep -n 'r8:8.13.19' /home/yourt/zchat-android/settings.gradle.kts (pin present; if bumped, must be >=8.13.19 — never fall back to the AGP default); ./gradlew :app:assembleZcashmainnetFossRelease completes without dex diagnostic spam or OO…  _(release_build_requirements)_
- [ ] **In-app update endpoint stale — users never prompted past 2.11.0 for 5 releases** — Compare curl -s https://api.zsend.xyz/app/version versionName against the latest tag from gh release view --repo decentrathai/zchat-android --json tagName — they must match (allow <=1h cron lag, or run ssh_moltbot "/usr/bin/node /home/molt…  _(release_build_requirements)_

### 🛠 Backend / infra  ·  8 case(s)

- [ ] **Venice AI models returned 400 'pricing not configured' for 105 of 113 models** — Backend: curl the /ai/models endpoint on the moltbot backend and verify it returns a large catalog (both text and image models) with zchat_model_type and zchat_pricing fields. Device: in the AI tab pick a non-seeded model (e.g. venice-sd35…  _(ai_and_calls)_
- [ ] **wallet-core WASM build break (over-gated crypto imports)** — In wallet-core run `cargo check --target wasm32-unknown-unknown` AND `cargo check --features native` — both must pass.  _(audit_pending_items)_
- [ ] **ZchatPreferencesTest suspend-call compile break blocked androidTest** — `./gradlew :ui-lib:compileZcashmainnetFossDebugAndroidTestSources` (or assembleAndroidTest) compiles green.  _(bug_campaign `v2.11.0`)_
- [ ] **Public relays silently stop live delivery — dedicated relay deployed** — Run the relay round-trip script (pattern of /tmp/relay_test.py + relay_burst.py): publish 12-event burst of kind-1059 to wss://relay.zsend.xyz → 12/12 received live in <2s; hold a sub idle 25s → still live; check container up on moltbot.  _(bug_campaign)_
- [ ] **Message-request prefs read on main thread (LOW)** — Debug build with StrictMode disk-read penalties enabled: open the chat list with pending message requests -> logcat shows no StrictMode DiskReadViolation from the message-request store on the main thread.  _(free_open_msg1 `#224`)_
- [ ] **Stale drifted per-package Cargo.lock in wallet-core** — test ! -f /home/yourt/zchat/packages/wallet-core/Cargo.lock && echo OK — only the workspace-root /home/yourt/zchat/Cargo.lock may exist.  _(orchard_vuln_audit)_
- [ ] **ui-lib unit-test suite un-compilable under -Werror after Kotlin 2.3 migration** — cd /home/yourt/zchat-android && ./gradlew :ui-lib:testZcashmainnetFossDebugUnitTest — must COMPILE under -Werror and run the full suite green (a compile failure in any test file is a regression of this bug).  _(orchard_vuln_audit)_
- [ ] **Landing-page deploy pipeline broken by Next 16.2.6 (next-on-pages ERESOLVE)** — cd /home/yourt/zchat/apps/landing && NEXT_STATIC_EXPORT=1 pnpm build — exits 0 producing out/; then (source ~/.claude/.secrets) CLOUDFLARE_API_TOKEN=$CLOUDFLARE_API_TOKEN_PAGES CLOUDFLARE_ACCOUNT_ID=$CLOUDFLARE_ACCOUNT_ID npx wrangler@4 pa…  _(release_build_requirements)_
